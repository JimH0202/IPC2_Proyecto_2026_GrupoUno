@model List<OrbitNet.Web.Models.ViewModels.BufferDto>
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<OrbitNet.Web.Controllers.SharedResource> Localizer

<div class="card table-card">
    <div class="card-header">
        <h2>@Localizer["BuffersHeader"]</h2>
        <span class="card-subtitle">@Localizer["BuffersDescription"]</span>
    </div>

    <div class="tbl-wrap">
        <table class="tbl tbl-relay">
            <thead>
                <tr>
                    <th>@Localizer["BufferId"]</th>
                    <th>@Localizer["Type"]</th>
                    <th>@Localizer["ItemsInQueue"]</th>
                    <th>@Localizer["CapacityPercentage"]</th>
                    <th>@Localizer["Actions"]</th>
                </tr>
            </thead>
            <tbody id="buffersTableBody">
                @foreach (var buffer in Model)
                {
                    <tr>
                        <td>@buffer.BufferId</td>
                        <td>@buffer.Type</td>
                        <td>@buffer.ItemsInQueue</td>
                        <td>
                            <div class="progress-cell">
                                <div class="progress-bar" style="width:@buffer.CapacityPercentage%"></div>
                                <span>@buffer.CapacityPercentage.ToString("0")% </span>
                            </div>
                        </td>
                        <td>
                            <button class="btn btn-danger btn-sm" type="button" data-action="clear-buffer" data-buffer-id="@buffer.BufferId">@Localizer["ClearBuffer"]</button>
                            <button class="btn btn-outline btn-sm" type="button" data-action="export-buffer" data-buffer-id="@buffer.BufferId">@Localizer["ExportCsv"]</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
